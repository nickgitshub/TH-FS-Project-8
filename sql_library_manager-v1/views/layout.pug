<!DOCTYPE html>
html
	head
		title Books
		link(href='/static/stylesheets/style.css', rel='stylesheet')

	body
		h1 Search
		form(method='post', action=`/books/search`)
			p
				label(for='search') Search database
				input#search(name='search', type='text', placeholder='Search for something')
			p
				input(type='submit', value='Search')
		form(method='post', action=`/books/reset`)
			p
				input(type='submit', value='Reset')

		block content
